<?php
/**
 * DEPRECATED in v2.0 — product columns are per-user and stored in `user_fields`.
 * Kept only as a historical reference for the old fixed TQB schema.
 * Runtime code no longer loads this file.
 */
return [];
